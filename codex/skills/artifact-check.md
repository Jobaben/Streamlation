# Skill: Artifact Check

Before executing a step, verify all prerequisite artifacts exist and are non-empty.

- If any required artifact is missing or empty:
  - STOP immediately.
  - Instruct the operator to run the prerequisite step that generates it.
